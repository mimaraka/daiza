# TODO（Daiza 実装タスク）

`docs/SPEC.md` の実装手順・仕様に基づくタスク一覧。上から順に進める想定。

## 1. 環境構築

- [x] Vite + React + TypeScript プロジェクトを初期化
- [x] TypeScript strict mode を有効化
- [x] ESLint + Prettier を設定（warningゼロ運用）
- [x] Tailwind CSS を導入
- [x] shadcn/ui を導入（`components.json`・`cn`ユーティリティ・テーマトークンを整備。個別コンポーネントは必要時に追加）
- [x] Vite の `base` を `/daiza/` に設定（GitHub Pages 用）
- [x] GitHub Pages へのデプロイ手順／ワークフローを整備（`.github/workflows/deploy.yml`）
- [x] `src/` のディレクトリ構成を作成（components / analysis / render / export / model / hooks / utils）

## 2. 型・状態基盤

- [ ] `model/types.ts` に画像・解析結果・パラメータの型を定義
- [ ] `model/state.ts` にアプリ状態の初期値・型を定義
- [ ] React Hooks による状態管理を実装（Redux 等は使わない）

## 3. UIレイアウト

- [ ] 左右2ペイン構成の `App.tsx` を作成
- [ ] `components/LeftPanel.tsx`：各種入力コントロールを配置
- [ ] `components/Preview.tsx`：画像プレビュー領域
- [ ] `components/ResultPanel.tsx`：解析結果一覧
- [ ] レスポンシブ対応（画面幅が狭い場合は上下配置へ切替）

## 4. PNG読み込み

- [ ] `analysis/imageLoader.ts`：PNG読み込みと `ImageData` 取得
- [ ] ドラッグ＆ドロップ対応
- [ ] ファイル選択対応
- [ ] RGBA判定（α=0を透明、α>0をアクリルとみなす）
- [ ] 画像はブラウザ内のみで処理（外部送信しない）

## 5. パラメータ入力

- [ ] フィギュア高さ(mm) 入力
- [ ] 板厚(mm) 入力（例: 2/3/5mm）
- [ ] 差込口幅(mm) 入力（例: 5/6/7）
- [ ] 安全率スライダー（1.0〜2.0、初期値1.3）
- [ ] 台座余白（左右余白 0〜30mm）

## 6. スケール計算

- [ ] フィギュア高さ(mm)と画像高さ(px)から `mm_per_pixel` を算出

## 7. 重心計算

- [ ] `analysis/centroid.ts`：α>0を均一密度とみなし画像モーメントで重心 `Cx=Σx/N`, `Cy=Σy/N`
- [ ] `analysis/contour.ts`：外形（輪郭）抽出

## 8. プレビュー・オーバーレイ描画

- [ ] `render/overlay.ts`：オーバーレイ描画ロジック
- [ ] 外形（半透明）
- [ ] 重心（赤丸）
- [ ] 差込口（青矩形）
- [ ] 台座（緑矩形）
- [ ] 支持範囲（オレンジ線）
- [ ] 重心からの鉛直線（点線）
- [ ] すべてリアルタイム更新

## 9. 表示操作

- [ ] ホイールズーム
- [ ] ドラッグパン
- [ ] Fit表示
- [ ] 100%表示

## 10. 差込口探索

- [ ] `analysis/slot.ts`：画像最下部から探索し、差込口幅が完全に収まる範囲のみ候補化
- [ ] 複数候補時は重心真下に最も近い位置を採用

## 11. 台座サイズ計算

- [ ] `analysis/base.ts`：支持多角形の考え方で台座幅を算出
- [ ] 「重心が支持範囲内」を最低条件とする
- [ ] 安全率を掛けて推奨台座幅を計算
- [ ] 推奨奥行を算出

## 12. 転倒シミュレーション

- [ ] `analysis/stability.ts`／`render/simulation.ts`：転倒角 `θ = atan(支持端距離 / 重心高さ)`
- [ ] 左右それぞれの転倒角を計算・表示

## 13. 解析パイプライン統合

- [ ] `hooks/useAnalysis.ts`：解析→状態更新→再描画のパイプラインを束ねる
- [ ] `utils/geometry.ts` / `utils/image.ts` の共通処理を実装

## 14. 結果表示

- [ ] 画像サイズ／実寸／重心座標(mm)
- [ ] 差込口中心／差込口幅
- [ ] 推奨台座幅／推奨奥行
- [ ] 転倒角(左)／転倒角(右)／安全率

## 15. SVGエクスポート

- [ ] `export/svg.ts`：外形・差込口・台座を含むSVGを実寸(mm)座標系で生成
- [ ] ブラウザからのダウンロードを実装

## 16. エラーハンドリング

- [ ] PNG読み込み失敗
- [ ] 非対応画像
- [ ] 透明画像
- [ ] 差込口が配置不可
- [ ] 台座計算不可
- [ ] 例外によるクラッシュを防止（UI上へ分かりやすく表示）

## 17. パフォーマンス最適化

- [ ] `useMemo` / `useCallback` による不要な再計算の抑制
- [ ] オーバーレイのみの再描画（画像解析全体を再実行しない）
- [ ] 3000px程度の画像でも快適に動作することを確認

## 18. リファクタリング・品質

- [ ] UI・画像処理・物理計算・描画の分離を再確認
- [ ] `any` 型の排除
- [ ] ESLint warningゼロ／Prettier準拠を確認
- [ ] 「なぜその処理が必要か」を中心としたコメント整備

## 将来拡張（バックログ）

- [ ] 複数差込口
- [ ] 円形台座
- [ ] 楕円台座
- [ ] 任意形状台座
- [ ] 金属スタンド対応
- [ ] アクリル以外の素材
- [ ] 密度マップ
- [ ] パーツ分割
- [ ] 複数PNG同時計算
- [ ] PWA化 / WebAssembly置き換え
